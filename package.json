{
    "name": "berlin-poket-guide",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "eleventy --input=src",
        "serve": "eleventy --serve --watch --input=src",
        "clean": "rm -rf _site"
    },
    "devDependencies": {
        "@11ty/eleventy": "^3.1.2"
    }
}
