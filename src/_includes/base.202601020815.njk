<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title or "Guía de viaje" }}</title>
  <meta name="description" content="{{ description or 'Itinerario y paradas día a día' }}">

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Tu CSS (IMPORTANTE: habilita el overlay clicable de las cards) -->
  <link rel="stylesheet" href="{{ '/style.css' | url }}">
</head>
<body>
  <header class="mb-4 border-bottom pb-2">
    <h1 class="h3 m-0 site-title">
      <a href="{{ '/' | url }}" class="text-decoration-none text-dark">Guía de viaje</a>
    </h1>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer class="mt-5 pt-3 border-top small text-muted">
    © {{ build.year }} — Hecho con Eleventy
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
